# v48-mobile-landscape-2026-07-12

## 版本註解

新增手機橫向遊玩模式。

## 改動

- 手機直向改為顯示「請旋轉成橫向」提示。
- 手機橫向不再被桌機限定遮罩擋住。
- 新增橫向手機專用 CSS：
  - 隱藏右側教練與牌局紀錄側欄，優先保留牌桌與操作列。
  - 縮小 AI 座位、公共牌、玩家手牌與 Pot。
  - 壓縮上方工具列與底部操作列。
  - 保留新手教學視窗並加上橫向手機緊湊樣式。
- 更新 viewport 支援 safe area。
- 同步更新 `docs/` 部署用檔案。
- 保持三個主檔結構，沒有新增額外資料夾。

## 驗證

- `node --check app.js`
- `node --check docs/app.js`
- 根目錄與 `docs/` 的 `index.html`、`styles.css`、`app.js` 已同步。
- 本機瀏覽器預覽通過：
  - 390x844 直向顯示旋轉提示。
  - 844x390 橫向可進入牌桌。
  - 橫向無水平或垂直 overflow。
  - 右側側欄隱藏、牌桌與操作列可見。
